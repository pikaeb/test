<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

<!--    <uses-feature-->
<!--        android:name="android.hardware.camera"-->
<!--        android:required="false" />-->
<!--    <uses-permission android:name="android.permission.CAMERA" />    &lt;!&ndash; 카메라 사용 허용 &ndash;&gt;-->
<!--    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> &lt;!&ndash; 13이상부터 필요한 권한(알림)&ndash;&gt;-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test_splash2">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!--        <activity android:name=".MainActivity"-->
<!--            android:launchMode="singleTask" />-->
    </application>

</manifest>